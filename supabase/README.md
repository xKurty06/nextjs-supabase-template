# Supabase

Use this folder for Supabase local development files.

- Put migration files in `supabase/migrations/`.
- Put seed data in `supabase/seed.sql`.
- Generate database types into `src/types/database.types.ts`.

This template does not include a database schema yet.
